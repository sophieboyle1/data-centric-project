<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add Manager</title>
    <!-- Internal CSS for styling the page -->
    <style>
        body {
            font-family: 'Arial', sans-serif;
            margin: 20px;
            background: #f4f4f4;
            color: #333;
        }
        .container {
            max-width: 600px;
            margin: 20px auto;
            padding: 20px;
            background: #fff;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }
        h1 {
            color: #333;
        }
        form {
            margin-top: 20px;
        }
        .form-group {
            margin-bottom: 10px;
        }
        label {
            display: block;
            margin-bottom: 5px;
        }
        input[type='text'],
        input[type='number'] {
            width: 100%;
            padding: 8px;
            margin-bottom: 10px;
            border: 1px solid #ddd;
            border-radius: 4px;
        }
        input[type='submit'] {
            width: 100%;
            padding: 10px;
            border: none;
            background-color: #5cb85c;
            color: white;
            cursor: pointer;
            border-radius: 4px;
        }
        input[type='submit']:hover {
            background-color: #4cae4c;
        }
        .error-message {
            color: #d9534f;
            margin-bottom: 20px;
        }
        .conditions {
            color: #555;
            background-color: #eee;
            padding: 10px;
            border-radius: 4px;
            margin-bottom: 20px;
            font-size: 14px;
        }
    </style>
</head>
<body>
    <!-- Main container for content -->
    <div class="container">
        <h1>Add Manager</h1>
        <div class="conditions">
            <p>Manager ID must be 4 characters</p>
            <p>Name must be more than 5 characters</p>
            <p>Salary must be between 30,000 and 70,000</p>
        </div>
        <!-- Error message section -->
        <% if (errors && errors.length > 0) { %>
            <div class="error-message">
                <ul>
                    <% errors.forEach(function(error) { %>
                        <li><%= error.msg %></li>
                    <% }); %>
                </ul>
            </div>
        <% } %>
        <!-- Form for adding a manager -->
        <form action="/managers/add/add" method="post">
            <div class="form-group">
                <label for="managerID">Manager ID</label>
                <input id="managerID" name="managerID" type="text" required />
            </div>
            <div class="form-group">
                <label for="managerName">Name</label>
                <input id="managerName" type="text" name="managerName" required />
            </div>
            <div class="form-group">
                <label for="managerSalary">Salary</label>
                <input id="managerSalary" type="number" name="managerSalary" required />
            </div>
            <input type="submit" value="Add">
        </form>
        <a href="/">Home</a>
    </div>
</body>
</html>
